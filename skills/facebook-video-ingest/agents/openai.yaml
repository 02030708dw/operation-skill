interface:
  display_name: "Facebook Video Ingest"
  short_description: "Run on-demand local HM video Workers and upload to R2"
  default_prompt: "Use $facebook-video-ingest to pair local Hermes and run a targeted HM video ingest task on demand."
